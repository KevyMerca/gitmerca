# Fish completion for git-cleanup
# Install: Copy to ~/.config/fish/completions/

complete -c git-cleanup -s h -l help -d "Show help message"
complete -c git-cleanup -s v -l version -d "Show version information"
complete -c git-cleanup -s y -l yes -d "Skip confirmation prompt"
